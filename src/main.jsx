import React from 'react'
import ReactDOM from 'react-dom/client'
import PartsCheckInSystem from './PartsCheckInSystem.jsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <PartsCheckInSystem />
  </React.StrictMode>,
)
